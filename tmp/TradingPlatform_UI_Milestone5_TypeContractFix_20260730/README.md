# UI Milestone 5 Type Contract Fix

Corrects the Institutional Intelligence opportunity queue to read AI/opportunity score from the canonical `OpportunityRecord.source_payload` rather than a nonexistent top-level `ai_score` field.
