import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import test from 'node:test';
const root=path.resolve(path.dirname(new URL(import.meta.url).pathname),'..');
const page=fs.readFileSync(path.join(root,'src','PortfolioIntelligenceRefinedPage.tsx'),'utf8');
const css=fs.readFileSync(path.join(root,'src','portfolio-intelligence-refined.css'),'utf8');
test('uses canonical portfolio intelligence APIs',()=>{assert.match(page,/portfolioIntelligenceApi\.positions/);assert.match(page,/portfolioIntelligenceApi\.snapshot/);assert.match(page,/portfolioIntelligenceApi\.generateSnapshot/)});
test('preserves optimistic version governance for actions',()=>{assert.match(page,/selected\.version/);assert.match(page,/portfolioIntelligenceApi\.action/);assert.match(page,/Reason for/)});
test('renders health exposure greeks decisions and alerts',()=>{for(const value of ['Portfolio Command Center','Decision Intelligence','Position Health Drivers','Aggregate Greeks','Sector Exposure','Active Alerts'])assert.match(page,new RegExp(value))});
test('does not submit broker orders',()=>{assert.doesNotMatch(page,/placeOrder|submitOrder|ibkr|broker.*submit/i)});
test('keeps layout route-local and responsive',()=>{assert.match(css,/\.pi-layout/);assert.match(css,/@media\(max-width:900px\)/);assert.doesNotMatch(css,/(^|\n)aside\s*\{/)});
