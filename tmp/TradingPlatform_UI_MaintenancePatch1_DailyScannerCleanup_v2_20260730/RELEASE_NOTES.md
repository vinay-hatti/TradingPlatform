# Release Notes

This version-specific patch replaces only `ui/workstation/src/pages.tsx` and is guarded by SHA-256 checks against the uploaded post-Milestone-9 baseline. It is repeat-safe and creates a timestamped rollback backup before applying.
