# Trading AI Control Center

Start the local browser interface:

```bash
./run_control_center.sh
```

The control center runs at:

```text
http://127.0.0.1:8501
```

It integrates the current Trading AI CLI rather than duplicating the strategy,
risk, execution, or reporting logic.
