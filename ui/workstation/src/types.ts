export type Envelope<T=unknown>={status?:string;request_id:string;generated_at?:string;data:T;metadata:Record<string,unknown>};
export type ArtifactState={exists:boolean;stale:boolean;modified_at:string|null;age_seconds:number|null;path:string};
export type Overview=Record<string,ArtifactState>;
export type Health={service:string;milestone:number;status:string};
export type Readiness={ready:boolean;components:Record<string,ArtifactState>};
export type WorkspaceKey='overview'|'scanner'|'portfolio'|'risk'|'execution'|'positions'|'exits'|'command';
export type RunStatus='QUEUED'|'RUNNING'|'SUCCEEDED'|'FAILED';
export type ScannerRun={run_id:string;kind:'DATA_REFRESH'|'DAILY_SCAN';status:RunStatus;created_at:string;started_at?:string|null;completed_at?:string|null;requested_by:string;request:Record<string,unknown>;exit_code?:number|null;stdout:string;stderr:string;report_date?:string|null;artifacts:Record<string,string>;summary:Record<string,any>};
export type RefreshMode='cache_only'|'refresh_missing'|'force_full';
export type ScannerUniverse={id:string;label:string;description:string;symbol_count:number;source:string;as_of_date?:string|null;maximum_symbols?:number|null;active:boolean};
export type DataRefreshRequest={data_scope:'underlying'|'options'|'all';universe:string;symbols:string[];start:string;end:string;refresh_mode:RefreshMode;minimum_bars:number;stale_after_days:number;minimum_coverage_pct:number;maximum_failed_symbols:number;continue_on_degraded_refresh:boolean;max_retries:number;retry_backoff_seconds:number;maximum_retry_backoff_seconds:number;retry_jitter_ratio:number;rate_limit_cooldown_seconds:number;circuit_breaker_threshold:number;circuit_breaker_cooldown_seconds:number;batch_size:number};
export type DailyScanRequest={universe:string;symbols:string[];start:string;end:string;minimum_score:number;top:number;pricing_dte:number;expiration_mode:'automatic'|'short'|'swing'|'medium'|'long'|'custom'|'fixed';minimum_dte:number;maximum_dte:number;maximum_expirations_per_symbol:number;maximum_trades_per_expiration:number;option_data_mode:'live'|'auto'|'proxy';liquidity_data_mode:'adaptive'|'strict';maximum_option_spread_pct:number;minimum_option_open_interest:number;minimum_option_volume:number;capital:number;risk_per_trade_pct:number;max_position_pct:number;take_profit_pct:number;stop_loss_pct:number;refresh_mode:RefreshMode;auto_refresh:boolean;minimum_refresh_coverage_pct:number;maximum_failed_refresh_symbols:number;continue_on_degraded_refresh:boolean;refresh_max_retries:number;refresh_retry_backoff_seconds:number;refresh_maximum_retry_backoff_seconds:number;refresh_retry_jitter_ratio:number;refresh_rate_limit_cooldown_seconds:number;refresh_circuit_breaker_threshold:number;refresh_circuit_breaker_cooldown_seconds:number};
export type ScannerResults={run:ScannerRun;recommendations:{metadata?:Record<string,any>;candidates?:any[]};trades:{metadata?:Record<string,any>;trades?:any[]}};
